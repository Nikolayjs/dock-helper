# Адрес dock-helper-api. По умолчанию — локальный бэкенд из соседнего репозитория.
# В проде фронт и API живут на одном домене, поэтому там значение задаётся при сборке образа.
VITE_API_BASE_URL=http://localhost:3000/api
